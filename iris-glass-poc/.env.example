# Copy this file to `.env.local` and fill in. Never commit `.env.local`.

# HERMES v0.4.x — token dedicado, escopo exclusivo /glass/hermes/*.
# O build:prod fixa VITE_HERMES_API_BASE na tailnet; o token vem daqui e é
# incorporado ao .ehpk privado. Nunca publicar o pacote fora do canal privado.
VITE_GLASS_API_TOKEN=
